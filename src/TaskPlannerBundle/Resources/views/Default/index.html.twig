{% extends '::base.html.twig' %}

{% block body -%}
    <div class="container">
        <div class="row">
            <div class="col-sm-2">
                {{ render(controller("TaskPlannerBundle:Category:index")) }}
            </div>
            <div class="col-sm-10">
                <div class="tab-content">
                    <div id="tasks" class="tab-pane fade in active">
                        {{ render(controller("TaskPlannerBundle:Task:index")) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
